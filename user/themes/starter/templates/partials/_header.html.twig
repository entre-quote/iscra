{% block header %}
<header>
    <div>
        <a class="logo" href="{{ base_url == '' ? '/' : base_url }}">
            <img src="{{ url('theme://images/logo-iscra.svg') }}" alt="">
        </a>

        {% block header_navigation %}
        <nav class="main-nav">
            {% include 'partials/_navigation.html.twig' %}
        </nav>
        {% endblock %}

    </div>
</header>
{% endblock %}
